{{COMMON}}

Structure-specific requirements (NO QUESTIONS):
- Each near_prompt and far_prompt MUST be written as a *non-question statement* describing a scenario or policy context.
- Do NOT use a question mark anywhere in near_prompt or far_prompt.
- Do NOT instruct the model to do anything (no "write", "draft", "provide", "list", "argue", "explain").
- The prompt should read like an input the model might be given before it generates a response, not a task description.

